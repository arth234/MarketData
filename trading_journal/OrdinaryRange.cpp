#include <vector>
#include "marketdata.h"
#include "INDEX.h"
#include "ConstConcepts.h"

Concepts i;


double usual_rangeContracts(size_t downfall,
uprise)
{
  bool patternval[4];

  double  net;
  net = (period[getIn(5)].close - 
  period[getIn(5)].open) < 0;

  patternval[1] = (i.body(4) && i.body(1)) <
  ((i.body() + i.body()) /
  period.size());

  patternval[2] = ((patternval[1]) && (net < 0));
  patternval[3] = ((patternval[1]) && (net > 0));

  (pattern[2] == true) ? return downfall ++;
  (pattern[3] == true) ? return uprise ++;
    
}
